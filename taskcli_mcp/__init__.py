"""MCP server consumer of the taskcli library.

This package is intentionally separate from `taskcli` itself: `taskcli` is the
library, and `taskcli_mcp` is one of potentially many programs that wrap it.
"""

__version__ = "0.1.0"
